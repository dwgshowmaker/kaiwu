# 代码规范

## 文档目标

本文档记录峡谷追猎项目在 `agent_diy` 方向上的开发规范。后续每次修改代码、训练流程、奖励设计或特征结构时，都要同步检查并更新本文档，确保实现、文档和实验记录保持一致。

---

## 一、开发边界

### 1. 默认开发目录

新策略、新特征、新奖励、新模型默认都放在 `agent_diy` 下实现。

`agent_ppo` 作为稳定参考基线，除非明确说明，不在迭代中修改。

### 2. 代码职责

| 路径 | 职责 |
| :-- | :-- |
| `agent_diy/agent.py` | Agent 推理、动作处理、模型保存加载 |
| `agent_diy/algorithm/algorithm.py` | PPO 损失、优化器更新、训练指标上报 |
| `agent_diy/conf/conf.py` | 特征维度、动作维度、PPO 超参数 |
| `agent_diy/feature/definition.py` | 数据结构、样本字段、GAE 计算 |
| `agent_diy/feature/preprocessor.py` | 特征处理、奖励塑形、局内状态缓存 |
| `agent_diy/model/model.py` | Actor-Critic 网络结构 |
| `agent_diy/workflow/train_workflow.py` | 环境交互、采样、终局奖励、监控上报 |
| `agent_diy/conf/monitor_builder.py` | 自定义监控面板 |

---

## 二、代码风格

### 1. 命名规范

- Python 文件、变量和函数使用 `snake_case`
- 类名使用 `PascalCase`
- 常量使用 `UPPER_SNAKE_CASE`
- 特征分组命名尽量和 `Config.FEATURES` 对齐，例如 `hero_feat`、`monster_feats`、`map_feat`

### 2. 注释规范

- 只在非显然逻辑前添加简短注释
- 奖励项、特征维度、动作维度必须有注释说明
- 不写“给变量赋值”这类低信息量注释

### 3. 导入规范

导入顺序建议为：

1. 标准库
2. 第三方库
3. 项目内部模块

示例：

```python
import os
import time

import numpy as np
import torch

from agent_diy.conf.conf import Config
```

---

## 三、特征规范

### 1. 特征维度必须单点维护

特征结构发生变化时，必须同步修改：

- `agent_diy/conf/conf.py` 中的 `FEATURES`
- `agent_diy/conf/conf.py` 中由 `FEATURES` 推导出的 `DIM_OF_OBSERVATION`
- `agent_diy/feature/definition.py` 中 `SampleData.obs`
- `agent_diy/feature/preprocessor.py` 中实际拼接出的特征长度

### 2. 特征拼接要求

特征拼接顺序必须稳定，不能在训练中动态变化。

推荐顺序：

1. hero 特征
2. monster 特征
3. treasure 特征
4. buff 特征
5. map 特征
6. legal action 特征
7. progress 特征

Phase 1 当前实际特征布局为 **105 维**：

| 分组 | 维度 | 说明 |
| :-- | --: | :-- |
| hero | 8 | 坐标、闪现冷却、buff、步数进度 |
| monsters | 16 | 2 只怪物，每只 8 维 |
| treasures | 10 | 最近 2 个宝箱，每个 5 维 |
| buffs | 10 | 最近 2 个 buff，每个 5 维 |
| local map | 49 | 局部地图下采样为 7x7 |
| legal action | 8 | 8 个移动动作合法性 |
| progress | 4 | 第二怪物、怪物加速、宝箱/buff 收集比例 |

### 3. 特征值范围

- 连续数值尽量归一化到 `[0, 1]` 或 `[-1, 1]`
- 布尔特征使用 `0.0 / 1.0`
- 缺失实体使用全零向量
- 不把原始大坐标直接塞进模型

### 4. 维度检查

`preprocessor.py` 中建议保留特征长度断言：

```python
assert len(feature) == Config.DIM_OF_OBSERVATION
```

如果后续训练环境对断言敏感，可以改成日志报错，但开发阶段优先保留断言。

---

## 四、奖励规范

### 1. 奖励设计原则

奖励要贴近比赛目标：

- 活得更久
- 拿更多宝箱
- 合理吃 buff
- 避免撞墙和原地抖动
- 后续阶段再鼓励合理闪现

Phase 1 当前奖励项：

| 奖励项 | 实现 |
| :-- | :-- |
| 生存奖励 | 每步 `+0.02` |
| 怪物距离塑形 | 最近怪物距离增加给正奖励，危险状态下权重更高 |
| 宝箱奖励 | 宝箱计数增量 `+1.2` |
| buff 奖励 | buff 计数增量 `+0.5`，危险时 `+0.7` |
| 卡住惩罚 | 连续原地不动从第 2 步开始惩罚 |
| 撞墙动作惩罚 | 最近导致原地不动的动作进入短期冷却，危险时额外惩罚 |
| 近身危险惩罚 | 怪物距离小于等于 5 格时惩罚，2 格内惩罚更强 |

### 2. 事件奖励优先用差分

以下奖励应基于计数差分：

- 宝箱奖励：`treasure_collected_count` 或 `treasures_collected` 的增量
- buff 奖励：`collected_buff` 的增量
- 闪现奖励：`flash_count` 的增量

不要只凭位置猜测事件是否发生。

### 3. 奖励状态缓存

`Preprocessor.reset()` 必须重置所有跨步缓存，例如：

- 上一步英雄位置
- 上一步最近怪物距离
- 上一步宝箱数
- 上一步 buff 数
- 上一步闪现次数
- 连续卡住计数

---

## 五、动作规范

### 1. Phase 1 仍保持 8 动作

Phase 1 只升级特征和奖励，不扩展动作空间。

当前动作空间仍为：

- `0-7`：八方向移动

### 2. Phase 1 安全动作先验

训练日志显示早期策略大量在 30 步内失败，主要问题不是训练链路报错，而是随机探索阶段离怪物过近、样本质量过差。

因此 Phase 1 允许在 `agent_diy/agent.py` 中加入轻量安全先验：

- 只在 `danger_level >= 0.25` 时生效
- 只在 8 移动动作内选择短视野逃生评分最高的 `safe_action`
- `safe_action` 评分必须考虑远离最近怪物、相邻格通行性、后续 3 格通路长度、局部开阔度和撞墙动作冷却
- 只混合动作概率，不改变 PPO 损失公式
- 先验权重最高不超过 `0.6`，避免长期压制策略学习

### 3. Phase 2 再扩展 16 动作

扩展闪现时必须同步修改：

- `Config.ACTION_NUM`
- `SampleData.legal_action`
- `SampleData.prob`
- Actor 输出维度
- `Preprocessor` 的合法动作掩码
- 闪现奖励与监控项

---

## 六、训练与监控规范

### 1. 训练流程

默认训练入口使用 `train_test.py`，并保持：

```python
algorithm_name = "diy"
```

### 2. 监控指标

新增奖励或行为后，应同步考虑是否需要上报监控。

Phase 1 建议关注：

- `reward`
- `episode_steps`
- `total_score`
- `treasure_count`
- `buff_count`
- `flash_count`
- `fail_rate`
- `stuck_count`
- `avg_min_monster_dist`
- `danger_level`
- `blocked_count`
- `danger_steps`
- `near_death_count`
- `safe_prior_count`
- `safe_action_count`

训练日志排查时，`[GAMEOVER]` 行至少应包含：

- `steps`
- `result`
- `sim_score`
- `total_reward`
- `treasure`
- `buff`
- `stuck`
- `blocked`
- `danger`
- `danger_steps`
- `near_death`
- `safe_prior`
- `safe_action`

---

## 七、验证规范

每轮代码修改后至少执行：

```bash
python -m py_compile agent_diy/agent.py agent_diy/algorithm/algorithm.py agent_diy/conf/conf.py agent_diy/feature/definition.py agent_diy/feature/preprocessor.py agent_diy/model/model.py agent_diy/workflow/train_workflow.py train_test.py
```

如果本地缺少训练依赖，例如 `torch`，需要在最终说明中明确记录无法完整运行的原因。

---

## 八、Git 规范

每完成一轮可独立验证的改动，都要执行：

```bash
git status
git add .
git commit -m "填写本次改动说明"
git push
```

实际提交时可以按路径精确提交，避免混入无关文件。

推荐提交粒度：

- 一次文档更新一个 commit
- 一次 Phase 实现一个 commit
- 一次 bugfix 一个 commit

提交信息示例：

```bash
git commit -m "add diy coding standards"
git commit -m "implement phase1 feature rewards"
git commit -m "fix diy reward state reset"
```

---

## 九、文档同步规范

每次改动后都要检查是否需要同步更新：

- `峡谷追猎/开发指南/PPO迭代开发方案.md`
- `峡谷追猎/开发指南/代码规范.md`
- 相关代码注释

如果代码实现和文档描述不一致，以本轮代码实现为准，并立即更新文档。
